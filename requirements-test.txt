pytest>=8.0
pytest-mock>=3.12
pytest-cov>=5.0
responses>=0.25
freezegun>=1.4
ruff>=0.15,<0.16  # 0.16.3 (2026-08-19) changed default lint rule selection, surfacing 369 pre-existing findings unrelated to any change — pin until those are triaged deliberately
mypy>=2.0
pylint>=4.0
codespell>=2.4
pip-audit>=2.10
